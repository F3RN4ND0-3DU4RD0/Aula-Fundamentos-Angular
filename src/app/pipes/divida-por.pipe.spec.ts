import { DividaPorPipe } from './divida-por.pipe';

describe('DividaPorPipe', () => {
  it('create an instance', () => {
    const pipe = new DividaPorPipe();
    expect(pipe).toBeTruthy();
  });
});
