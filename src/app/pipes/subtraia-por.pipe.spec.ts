import { SubtraiaPorPipe } from './subtraia-por.pipe';

describe('SubtraiaPorPipe', () => {
  it('create an instance', () => {
    const pipe = new SubtraiaPorPipe();
    expect(pipe).toBeTruthy();
  });
});
